// Legal context — jurisdiction-specific reference for the agent.
//
// ⚠ PLACEHOLDER — NOT YET FILLED IN.
//
// Until this file is populated, the agent must emit a strong, prominent
// warning at the start of every onboard, asking the lawyer to fill it in.
// Detection is the literal string "PLACEHOLDER — NOT YET FILLED IN" anywhere
// in the file. Without this content, the agent will fall back to generic
// guesses and may produce citations, deadlines, or procedural references that
// are wrong for this jurisdiction.
//
// Replace the placeholder content under each heading below with the real
// jurisdiction-specific rules. Keep the structure — the agent reads section
// by section.

= Legal context

== Jurisdiction

// Country, province/state, federal vs unitary, the legal tradition (common
// law / civil law / hybrid), the principal codes or statutes that govern
// procedure.

_TODO_

== Court hierarchy

// List the courts the lawyer practices in, in order of seniority. Note any
// jurisdictional thresholds (monetary limits, subject-matter restrictions),
// territorial divisions, and specialist courts (labour, tax, equality,
// children's, etc.).

_TODO_

== Citation conventions

// How cases are cited in this jurisdiction. Neutral citation patterns. How
// statutes are cited (long title, short title, year). How regulations and
// subordinate legislation are cited. Examples that show the expected format.

_TODO_

== Date and currency formatting

// Long-form date (e.g. "29 April 2026"), short-form date, frontmatter
// convention. Currency symbol, decimal separator, thousands separator,
// thin-space conventions.

_TODO_

== Court days vs calendar days

// Which deadlines are "court days" (excluding weekends and public holidays)
// and which are calendar days. List public holidays for the year if helpful,
// or point to an authoritative source.

_TODO_

== Common rule-based deadlines

// Standard procedural windows by court level — e.g. notice of intention to
// defend, plea, replication, discovery, heads of argument. Include the rule
// reference where possible so the agent can cite the source.

_TODO_

== Document-format conventions

// Pleadings layout (numbered paragraphs, prayers, signature block).
// Letter conventions (your reference / our reference, salutations, sign-off).
// Affidavit / declaration form requirements (commissioner of oaths block,
// jurat, attestation language).

_TODO_

== Privilege, professional ethics, regulatory bodies

// Brief notes on privilege rules, the regulatory body or bodies (e.g. Legal
// Practice Council, Bar Council, Law Society), CPD requirements, conflict
// of interest rules worth flagging.

_TODO_

== Anything else jurisdiction-specific

// Anything that doesn't fit the above but matters — interpretation rules,
// transitional provisions in force, key recent reforms.

_TODO_
